{
  "_comment": "Always-on critical safety baseline (plan.md Phase 2). Merged into EVERY customer config and not disableable in pilots, so a narrow user policy (e.g. violence-only) can never hide a universal critical threat. Every rule here is still verified by TrueSight (the VLM gate) before it reaches the operator. A detector only contributes if the camera enables it.",
  "use_case_id": "baseline_critical_v1",
  "rules": [
    {"name": "baseline_weapon", "trigger": {"detector": "weapons"}, "priority": "critical"},
    {"name": "baseline_violence", "trigger": {"detector": "violence"}, "priority": "critical"},
    {"name": "baseline_camera_tampering", "trigger": {"detector": "camera_tampering"}, "priority": "high"},
    {"name": "baseline_fire_smoke", "trigger": {"detector": "fire_smoke"}, "priority": "critical"},
    {"name": "baseline_person_fall", "trigger": {"detector": "person_fall"}, "priority": "critical"},
    {"name": "baseline_crowd_surge", "trigger": {"detector": "crowd_surge"}, "priority": "high"}
  ]
}
