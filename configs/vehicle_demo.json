{
  "name": "Vehicle Demo",
  "notify": "console",
  "configured": true,
  "cameras": [
    {
      "id": "gate",
      "source": "packaging/demo_data/clips/entrance.mp4",
      "config": "configs/vehicle_demo.rules.json",
      "vehicle_zones": "configs/vehicle_demo.zones.json"
    }
  ],
  "_notify_comment": "Never put a real notify spec in a tracked config: it embeds a live bot/auth token and this repository is public. Set alert destinations in the app (Settings -> Alerts), which writes to the per-user data directory, or pass --notify on a dev run."
}
