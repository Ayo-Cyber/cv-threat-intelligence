<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Hardened-runtime entitlements for the signed Argus bundle (EP-05-T2).

     Notarisation requires the hardened runtime; these three exceptions are
     the standard set a PyInstaller + QtWebEngine app cannot run without:
     Python's ctypes/JIT'd stubs need unsigned executable memory, QtWebEngine's
     V8 needs JIT, and the bundle's mixed-origin dylibs (torch, cv2, Qt) need
     library validation off. Nothing here weakens sandboxing the app had —
     it never had entitlement-based sandboxing; alerts stay on-device. -->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
    <key>com.apple.security.cs.allow-jit</key><true/>
    <key>com.apple.security.cs.disable-library-validation</key><true/>
    <key>com.apple.security.device.camera</key><true/>
</dict>
</plist>
